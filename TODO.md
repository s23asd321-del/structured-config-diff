# TODO

- Keep the first release focused on JSON and TOML structured diffs.
- Add more tests around deeply nested array changes.
- Add examples for `--ignore`, `--no-values`, and `--strict-type`.
- Consider a compact Markdown table renderer after the core model stabilizes.
- Improve documentation for CI usage patterns.
- Review wording before public release to avoid overstating safety, security, or compliance capability.
- Review `docs/github-upload-checklist.md` before the first public upload.
